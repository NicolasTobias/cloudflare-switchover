apiVersion: v1
kind: Service
metadata:
  name: cloudflare-switchover
  labels:
    app: cloudflare-switchover
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: cloudflare-switchover
